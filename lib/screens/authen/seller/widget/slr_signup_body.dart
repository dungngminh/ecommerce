import 'package:flutter/material.dart';

class SellerSignUpBody extends StatelessWidget {
  const SellerSignUpBody({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}